on:
  push:
  deployment:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy Stage
      uses: fjogeleit/http-request-action@v1
      with:
        url: 'https://entity/auth/login'
        method: 'POST'
        customHeaders: '{"Content-Type": "application/json"}'
        data: '{"name": "admin","password": "123"}'
