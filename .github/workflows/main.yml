run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Login
        id: loginRequest
        uses: fjogeleit/http-request-action@v1
        with:
          url: "https://hackathon.acumatica.com/Theta/entity/auth/login"
          method: 'POST'
          data: '{"name" : "githubservice","password" : "Theta","company" : "Company","branch" : ""}'
      - name: Show Response
        run: |
          echo ${{ steps.loginRequest.outputs.response }}
          echo ${{ steps.loginRequest.outputs.headers }}
          echo ${{ steps.loginRequest.outputs.cookies }}
          echo ${{ fromJson(steps.loginRequest.outputs.response).field_you_want_to_access }}
      
