run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
env:
  test: Monday
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Show Response
        run: |
          echo ${{ env.test }}
          env.test: Tuesday
          echo ${{ env.test }}
      
